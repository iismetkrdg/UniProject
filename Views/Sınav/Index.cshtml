@model IPagedList<EduProject.Models.Sınav>

@using X.PagedList.Web.Common;

@{
    ViewData["Title"] = "Eski Sınavlar";
}

<h1>Eski Sınavlar</h1>



<div class="row">
    <p class="text-center h3">
        <a asp-action="Create" class=" text-warning nav-link">Sınav Paylaş</a>
    </p>
    @foreach (var item in Model) {
        <div class="col-md-3">
            <div class="card mt-2">
                <div class="card-body">
                    <p class="card-title">@Html.DisplayFor(modelItem => item.Name)</p>
                    <p class="card-text">@Html.DisplayFor(modelItem => item.DersAdı)</p>
                    <p class="card-text"><a target="_blank" href="~/images/@Html.DisplayFor(modelItem => item.FileName)">Belgeye git</a></p>
                </div>
            </div>
        </div>
    }   
</div>

<div class="position-absolute start-50 translate-middle bottom-0 mb-5">
    @Html.PagedListPager((IPagedList)Model, page => Url.Action("Index",
        new
            {
                page
        }),
        new PagedListRenderOptions
        {
            LiElementClasses = new string[] { "page-item" },
            PageClasses = new string[] { "page-link" },
            Display = PagedListDisplayMode.IfNeeded

            })
</div>
